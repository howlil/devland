{
  "name": "devland",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --test"
  },
  "devDependencies": {
    "ajv": "^8.0.0",
    "yaml": "^2.0.0"
  }
}
